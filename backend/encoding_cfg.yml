max_num_buildings: 10 # Maximum number of buildings in the parcel design
length_design: 20 # Number of cells along one side (will be calculated dynamically in training)
max_num_floors: 10 # Maximum number of floors per building

# Physical scale parameters (FIXED VALUES - DO NOT CHANGE)
# These are overridden in run_sail_training.py based on parcel size
xy_scale: 3.0  # FIXED: 3 meters per cell
z_scale: 3.0   # FIXED: 3 meters per floor

# Parcel size parameters (in meters) - will be overridden per run
parcel_width_m: 60.0   # Default parcel width
parcel_height_m: 60.0  # Default parcel height